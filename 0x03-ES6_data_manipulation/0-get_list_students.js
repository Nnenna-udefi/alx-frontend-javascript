export default function getListStudents() {
  const list = new Array();
  list[0] = { id: 1, firstName: 'Guillaume', location: 'San Francisico' };
  list[1] = { id: 2, firstName: 'James', location: 'Columbia' };
  list[2] = { id: 5, firstName: 'Serena', location: 'San Francisco' };

  return list;
}
